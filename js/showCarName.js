let {vechicleNames, bikeNames} = require("./carname"); 
// It requires so the node search and returns it. That's why it logs the objects
 // here importCarNames = exported value.
// console.log(vechicleNames, bikeNames);
